# UCHIMI: Слайдова презентация (7 слайда)

## СЛАЙД 1: ЗАГЛАВИЕ

**Заглавие:** Uchimi — Платформа за управление и визуализация на данни

**Подзаглавие:** Автоматично генериране на интерактивни диаграми от курирани SQL модели

**На дъното:** Дата, автор: [Ваше име], Университет: [Вашия университет]

**Design:** Снимка или логото на проекта (React + Node.js икони)

---

## СЛАЙД 2: ПРОБЛЕМ И РЕШЕНИЕ

### Проблем (лева половина)
- Данните са в базата — но не са визуални.
- Всеки път трябва да напишеш SQL за различен анализ.
- Не-техническите потребители не разбират SQL.
- Процесът е бавен: SQL → преглед → диаграма → отчет.

### Решение (дясна половина)
- Uchimi автоматизира целия поток.
- Един клик = генерирана интерактивна диаграма.
- Статични HTML отчети за лесен sharing.
- Модели са повторни — пиши SQL един път, визуализирай많은 пъти.

---

## СЛАЙД 3: АРХИТЕКТУРА И ТЕХНОЛОГИЧЕН СТЕК

### Диаграма (левяра половина)
```
ПОТРЕБИТЕЛ (Браузър)
       ↑↓
  React Frontend (порт 3000)
       ↑↓
 Express API (порт 4000)
       ↑↓
   chartGenerator.js
       ↑↓
   SQLite Database
```

### Технологии (дясна половина)
| Слой | Технология |
|------|-----------|
| Frontend | React.js, React Router |
| Backend | Node.js, Express.js |
| API | REST (JSON) |
| Диаграми | Chart.js (CDN) |
| База | SQLite |
| Язык | JavaScript |

---

## СЛАЙД 4: ТРИЛАЕН МОДЕЛ НА ДАННИ

### Слой 1: RAW DATABASE
```
Employee → Order → Product → Customer
  ↓ SQL трансформация
```

### Слой 2: STAGING (Преобработени таблици)
```
stg_employee ← SELECT Id AS employee_id, 
                      LastName AS last_name, ...
stg_order ← SELECT id AS order_id, ...
stg_product ← SELECT ProductID AS product_id, ...
  ↓ JOIN и бизнес логика
```

### Слой 3: CURATED (Готови за анализ)
```
curated_employee_order ← SELECT e.*, o.* FROM stg_employee JOIN stg_order
curated_product_inventory ← SELECT p.*, c.* FROM stg_product...
  ↓ Автоматична визуализация
```

### Резултат: Интерактивни диаграми в браузър

---

## СЛАЙД 5: ПОТОК НА ГЕНЕРИРАНЕ НА ДИАГРАМИ

### Стъпки

```
1. РАЗВОЙЧИК
   ├─ Създава SQL в models/curated/
   └─ Създава JSON метаданни

2. BACKEND ОБРАБОТКА
   ├─ POST /curated-models с SQL
   ├─ Изпълнява SQL срещу SQLite
   ├─ Взема ДО 10,000 редове
   └─ Анализира типове колони

3. ГЕНЕРИРАНЕ НА ДИАГРАМИ
   ├─ detectType() → number/string/date
   ├─ generateCharts() → создава дескриптори
   ├─ generateHTMLReport() → съставя HTML
   └─ saveCharts() → записва на диск

4. ВИЗУАЛИЗАЦИЯ
   ├─ Frontend открива: GET /curated-model/:name/charts
   ├─ Браузър показва статичния HTML
   ├─ Chart.js инициализира с данните
   └─ ПОТРЕБИТЕЛ вижда интерактивни диаграми
```

---

## СЛАЙД 6: LIVE DEMO (говор + екран-шот)

### Команди (за тялото на презентацията)
```powershell
# Терминал 1: Backend
cd backend
node server.js
# Вижаш: Server running on http://localhost:4000

# Терминал 2: Frontend
cd giiho-frontend
npm start
# Браузър отваря http://localhost:3000

# Регенериране на диаграмите:
cd backend
node regenerate-all-charts.js
# Вижаш: Found 2 curated models → Rows fetched: 830 → Charts saved
```

### Визуален поток
1. Покажи `models/curated/curated_employee_order.sql` — JOIN на employee и order.
2. Покажи метаданни `curated_employee_order.json` — 19 колони, типове.
3. Пусни генериране — конзола показва "Rows fetched: 830", "Generated 3 charts".
4. Отвори браузър → localhost:3000 → View Charts → Покажи 3 диаграмата (distribution, category, timeline).

### Интерактивност
- Хвърляй мишата над диаграмите → вижаш value, %
- Това е Chart.js в действие.

---

## СЛАЙД 7: ОГРАНИЧЕНИЯ, ПОДОБРЕНИЯ И СЛЕДВАЩИ СТЪПКИ

### ТЕКУЩИ ОГРАНИЧЕНИЯ

| Ограничение | Почему | Решение |
|------------|--------|--------|
| SQLite | Demo база, не е масивно-паралелна | Мигрирай на PostgreSQL/MySQL |
| Капачка 10K редове | Памет при диаграмни вычисления | SQL агрегация (GROUP BY) преди генериране |
| Базови диаграми | Само histogram, pie, timeline | Добави scatter, box plot, heatmap |
| Без auth | Всеки може да генерира/редактира | Добави потребителски login и rights |
| Без тестване | Ручна проверка | Unit тестове за chartGenerator, E2E тестове |

### ПРЕПОРЪЧАНИ ПОДОБРЕНИЯ

**Краткосрочни (1–2 седмици):**
- Добави unit тестове за `chartGenerator.js`.
- Добави validation на SQL (защита от injection).
- Визуализирай зависимости между куриране моделите (DAG).

**Среднесрочни (месец):**
- Миграция на frontend на TypeScript.
- Добави автоматично versioning на модели.
- Потребителски профили и permissions.

**Дългосрочни (квартал+):**
- Support на различни БД (PostgreSQL, MySQL, BigQuery).
- Advanced диаграми (Plotly вместо Chart.js).
- Scheduling и automations (дневни, преди-реглементирани отчети).
- Експорт на отчети (PDF, Excel).

### ЗАКЛЮЧЕНИЕ

Uchimi е функционален прототип, който демонстрира:
✅ Многослойна архитектура (RAW → Staging → Curated)
✅ Автоматична генерираща диаграма
✅ RESTful API и интерактивен frontend
✅ SQL трансформации и бизнес логика

Той е готов за:
- **Демонстрация:** локално работи без конфигурация.
- **Разширяване:** лесно добавяне на нови типове диаграми, бази.
- **Реал-свят:** с малки промени (auth, scaling, CI/CD).

---

# БОНУС: КРАТКА ШПАРГАЛКА ЗА ГОВОРЕНЕ (2–3 минути)

### Вводна 30 секунди:
"Uchimi е платформа за управление на данни, която автоматизира визуализацията на курирани SQL модели. През три слоя — сира база, staging трансформации и curated комбинирани модели — подготвяме данни и генерираме диаграми с един клик."

### Архитектура 1 минута:
"React frontend на порт 3000 комуникира с Node Express API на порт 4000. Когато запазиш курирана модел, backend изпълнява SQL срещу SQLite, взема пълните редове, анализира типовете колони, генерира подходящи диаграми и записва статичен HTML файл. Диаграмите използват Chart.js библиотека за интерактивност."

### Live демо 2 минути:
1. Покажи SQL модела.
2. Пусни регенериране (backend сървър показва progress).
3. Отвори браузър, покажи диаграмите.

### Закриване 30 секунди:
"Uchimi демонстрира пълна изчислителна архитектура: от данни к обработка до визуализация. Готов е за разширяване и реален свят употреба."

---

## ДОПЪЛНИТЕЛНИ РЕСУРСИ ДА СА РАЗПЕЧАТАШ

Файл за референция по време на презентация:
- `PRESENTATION_FULL_EXPLANATION_BG.md` — пълния анализ на проекта.
- `backend/server.js` — главния сървър, endpoints.
- `backend/chartGenerator.js` — логиката за генериране.
- `models/curated/curated_employee_order.sql` — пример на SQL модел.

Когато задават въпроси, можеш да кажеш:
- "Видя в backend/server.js, че endpoint...".
- "Логиката е в chartGenerator.js, функция...".
- "SQL моделите са в models/curated/".

Good luck на презентацията! 🚀

